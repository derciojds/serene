import { SectionDescCaption } from './SectionDescCaption';
import { SectionDescParagraph } from './SectionDescParagraph';
import { SectionDescRoot } from './SectionDescRoot';
import { SectionDescTitle } from './SectionDescTitle';

export const SectionDesc = {
  root: SectionDescRoot,
  caption: SectionDescCaption,
  title: SectionDescTitle,
  paragraph: SectionDescParagraph,
};
